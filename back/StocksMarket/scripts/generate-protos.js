// This script is a placeholder for proto generation
// In Node.js with @grpc/proto-loader, we load protos dynamically at runtime
// No code generation step is needed

console.log('✓ Proto files are loaded dynamically at runtime');
console.log('✓ No code generation needed with @grpc/proto-loader');
console.log('✓ Proto definitions ready to use');

